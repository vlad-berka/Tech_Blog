<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vlad's Tech Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="container-fluid">
    <nav class="navbar text-white justify-content-between">
      <a class="navbar-brand branding" href="/">VLAD'S TECH BLOG!</a>
      <div class="justify-content-right branding">
        <a href="/">HOME</a> |
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">MY DASHBOARD</a> |
        <button class="no-button" id="logout">LOGOUT</button>
        {{else}}
        <a href="/login">MY DASHBOARD</a> |
        <a href="/login">LOGIN</a>
        {{/if}}
      </div>
    </nav>
    
    {{#if render_image}}
    {{!-- Background image --}}
    <header class="masthead img_box_background" style="background-image: url('assets/notebook_background_image.jpg')">
      <div class="container position-relative px-4 px-lg-5">
          <div class="row gx-4 gx-lg-5 justify-content-center ">
              <div class="col-md-10 col-lg-8 col-xl-7 title_container justify-content-center">
                  <div class="site-heading justify-content-center">
                      <h1 class="justify-content-center">VLAD'S TECH BLOG</h1>
                      <span class="subheading italicized">For all things Dev Tech and IT</span>
                  </div>
              </div>
          </div>
      </div>
    </header>
    {{/if}}

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
</body>

</html>
